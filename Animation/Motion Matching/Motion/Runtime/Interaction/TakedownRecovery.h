#pragma once

#include "../Core/MMModule.h"

namespace motion {
// Blueprint module 140: Returns both characters to valid states.
const ModuleDescriptor &module_takedownrecovery();
} // namespace motion
